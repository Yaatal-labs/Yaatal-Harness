// yaatal-feed/src/hydrators/mod.rs
//
// X has 7 hydrators: CoreData, Gizmoduck, InNetwork, Subscription, VF, VideoDuration.
// YOKK will need: AuthorInfo, VoiceMetadata, LanguageDetection.
// These get implemented when wiring to Turso during Loco integration.
// The pipeline works without hydrators — they just enrich candidates.
